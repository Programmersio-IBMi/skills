# Drafts in progress. Skills here are NOT published — they are excluded from
# every manifest, hook, and the README. Promote a skill out by moving it into
# the parent `skills/` directory and registering it in the manifests.
